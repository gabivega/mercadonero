(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
    get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
  }) : x)(function(x) {
    if (typeof require !== "undefined") return require.apply(this, arguments);
    throw Error('Dynamic require of "' + x + '" is not supported');
  });
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));

  // src/components/CollateralHoldCard.jsx
  var import_react = __toESM(__require("react"), 1);
  var import_axios = __toESM(__require("axios"), 1);
  var import_react_router_dom = __require("react-router-dom");
  var import_react_auth = __require("@privy-io/react-auth");
  var import_sweetalert2 = __toESM(__require("sweetalert2"), 1);
  var import_lucide_react = __require("lucide-react");
  var import_LoadingSpinner = __toESM(__require("./LoadingSpinner"), 1);
  var import_jsx_runtime = __require("react/jsx-runtime");
  var import_meta = {};
  function CollateralHoldCard({ order, role = "buyer", onUpdate }) {
    const { getAccessToken } = (0, import_react_auth.usePrivy)();
    const navigate = (0, import_react_router_dom.useNavigate)();
    const expiresAt = order.collateralHold?.expiresAt;
    const reserveUsd = order.collateralHold?.reserveUsd || 0;
    const [now, setNow] = (0, import_react.useState)(Date.now());
    const [loading, setLoading] = (0, import_react.useState)(false);
    const timerRef = (0, import_react.useRef)(null);
    const isDark = typeof document !== "undefined" && document.documentElement.classList.contains("dark");
    (0, import_react.useEffect)(() => {
      timerRef.current = setInterval(() => setNow(Date.now()), 1e3);
      return () => clearInterval(timerRef.current);
    }, []);
    const msLeft = expiresAt ? new Date(expiresAt).getTime() - now : 0;
    const expired = msLeft <= 0;
    const minutes = Math.max(0, Math.floor(msLeft / 6e4));
    const seconds = Math.max(0, Math.floor(msLeft % 6e4 / 1e3));
    const display = expired ? "00:00" : `${String(minutes).padStart(2, "0")}:${String(seconds).padStart(2, "0")}`;
    const handleRetry = async () => {
      setLoading(true);
      try {
        const token = await getAccessToken();
        const { data } = await import_axios.default.post(
          `${import_meta.env.VITE_SERVER_URL}/api/order/${order._id}/retry-collateral`,
          {},
          { headers: { Authorization: `Bearer ${token}` } }
        );
        import_sweetalert2.default.fire({
          icon: "success",
          title: "\xA1Orden activada!",
          text: "Tu colateral fue congelado y el comprador ya puede pagar.",
          confirmButtonColor: "#3483fa"
        });
        onUpdate?.();
      } catch (error) {
        const msg = error.response?.data?.message || "No se pudo activar la orden.";
        import_sweetalert2.default.fire({
          icon: "error",
          title: "No se pudo activar",
          text: msg,
          confirmButtonColor: "#3483fa"
        });
      } finally {
        setLoading(false);
      }
    };
    const handleCancelHold = async () => {
      const confirm = await import_sweetalert2.default.fire({
        icon: "warning",
        title: role === "seller" ? "\xBFRechazar esta venta?" : "\xBFBuscar en otro vendedor?",
        text: role === "seller" ? "Si no pod\xE9s cubrir esta garant\xEDa, rechaz\xE1 la venta para que el comprador pueda buscar este producto en otro vendedor de inmediato, en vez de esperar a que expire la orden." : "Cancelar\xE1s esta espera y podr\xE1s buscar este producto en otro vendedor. No se te descontar\xE1 nada.",
        showCancelButton: true,
        confirmButtonText: role === "seller" ? "S\xED, rechazar venta" : "S\xED, buscar otro",
        cancelButtonText: "Cancelar",
        confirmButtonColor: "#F26722",
        cancelButtonColor: isDark ? "#27272a" : "#6b7280"
      });
      if (!confirm.isConfirmed) return;
      setLoading(true);
      try {
        const token = await getAccessToken();
        await import_axios.default.patch(
          `${import_meta.env.VITE_SERVER_URL}/api/order/${order._id}/cancel-collateral-hold`,
          {},
          { headers: { Authorization: `Bearer ${token}` } }
        );
        import_sweetalert2.default.fire({
          icon: "success",
          title: "Espera liberada",
          text: role === "seller" ? "Rechazaste la venta. El comprador fue notificado y podr\xE1 buscar este producto en otro vendedor." : "Pod\xE9s buscar este producto en otro vendedor.",
          confirmButtonColor: "#3483fa"
        });
        if (role === "buyer") {
          navigate("/compras");
        } else {
          onUpdate?.();
        }
      } catch (error) {
        import_sweetalert2.default.fire({
          icon: "error",
          title: "Error",
          text: error.response?.data?.message || "No se pudo liberar la espera.",
          confirmButtonColor: "#3483fa"
        });
      } finally {
        setLoading(false);
      }
    };
    const goToWallet = () => navigate("/billetera");
    return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("section", { className: "bg-white dark:bg-[#121212] p-6 rounded-2xl border dark:border-zinc-800 overflow-hidden", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "h-1.5 w-full bg-gradient-to-r from-amber-400 via-orange-400 to-[#F26722] -mt-6 mb-6" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "flex items-start gap-4", children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: `p-3 rounded-2xl shrink-0 ${expired ? "bg-red-50 dark:bg-red-900/20 text-red-500" : "bg-amber-50 dark:bg-amber-900/20 text-amber-500"}`, children: expired ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.AlertTriangle, { size: 26 }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.Hourglass, { size: 26 }) }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "flex-1", children: [
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("h3", { className: "font-black uppercase tracking-tight dark:text-white text-sm flex items-center gap-2", children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.ShieldCheck, { size: 16, className: "text-[#F26722]" }),
            role === "seller" ? "Venta en espera por garant\xEDa" : "Esperando que el vendedor deposite su garant\xEDa"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { className: "text-sm text-zinc-600 dark:text-zinc-300 mt-2", children: role === "seller" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            "El comprador quiere comprarte por",
            " ",
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("b", { className: "text-[#F26722]", children: [
              "US$ ",
              reserveUsd.toFixed(2)
            ] }),
            " de garant\xEDa, pero no ten\xE9s saldo libre suficiente. Deposit\xE1 esa garant\xEDa antes de que venza el plazo para no perder la venta."
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_jsx_runtime.Fragment, { children: "El vendedor necesita depositar su fondo de garant\xEDa para activar tu compra. Por si eleg\xEDs esperar, no se descontar\xE1 nada hasta que el vendedor confirme." }) }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "mt-4 flex flex-wrap items-center gap-3", children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: `px-4 py-2 rounded-xl font-mono text-xl font-black tracking-widest ${expired ? "bg-red-50 dark:bg-red-900/20 text-red-500" : "bg-amber-50 dark:bg-amber-900/20 text-amber-600"} `, children: display }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "text-xs text-zinc-500 dark:text-zinc-400", children: expired ? "Plazo vencido" : `para depositar la garant\xEDa de protecci\xF3n de compra.` })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "mt-5 flex flex-wrap gap-3", children: role === "seller" ? /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
              "button",
              {
                onClick: goToWallet,
                className: "flex items-center gap-2 bg-[#F26722] hover:bg-[#d95514] text-white px-4 py-2.5 rounded-xl font-semibold text-sm transition-colors",
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.ExternalLink, { size: 16 }),
                  " Ir a depositar garant\xEDa"
                ]
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
              "button",
              {
                onClick: handleRetry,
                disabled: loading || expired,
                className: "flex items-center gap-2 bg-[#3483fa] hover:bg-blue-600 text-white px-4 py-2.5 rounded-xl font-semibold text-sm transition-colors disabled:opacity-50 disabled:cursor-not-allowed",
                children: [
                  loading ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_LoadingSpinner.default, { size: "sm" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.RefreshCw, { size: 16 }),
                  "Deposit\xE9, activar orden"
                ]
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(
              "button",
              {
                onClick: handleCancelHold,
                disabled: loading || expired,
                className: "flex items-center gap-2 border border-red-300 dark:border-red-800 text-red-500 hover:bg-red-50 dark:hover:bg-red-900/20 px-4 py-2.5 rounded-xl font-semibold text-sm transition-colors disabled:opacity-50",
                children: [
                  loading ? /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_LoadingSpinner.default, { size: "sm" }) : /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.XCircle, { size: 16 }),
                  "Rechazar esta venta"
                ]
              }
            )
          ] }) : /* @__PURE__ */ (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "inline-flex items-center gap-2 text-xs bg-blue-50 dark:bg-blue-900/20 text-blue-600 dark:text-blue-400 px-3 py-2 rounded-lg", children: [
              /* @__PURE__ */ (0, import_jsx_runtime.jsx)(import_lucide_react.Hourglass, { size: 14 }),
              " Sin cargo hasta que el vendedor confirme la garant\xEDa."
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
              "button",
              {
                onClick: handleCancelHold,
                disabled: loading,
                className: "flex items-center gap-2 border border-red-300 dark:border-red-800 text-red-500 hover:bg-red-50 dark:hover:bg-red-900/20 px-4 py-2.5 rounded-xl font-semibold text-sm transition-colors disabled:opacity-50",
                children: "Buscar en otro vendedor"
              }
            )
          ] }) })
        ] })
      ] }),
      role === "buyer" && !expired && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("p", { className: "mt-4 text-[11px] text-zinc-400 dark:text-zinc-500 border-t dark:border-zinc-800 pt-3", children: "Si prefer\xEDs esperar, esta orden se activar\xE1 autom\xE1ticamente apenas el vendedor confirme su garant\xEDa. Si no lo hace antes del plazo, se cancelar\xE1 sin costo para vos." })
    ] });
  }
})();
